version: '3.8'

services:
  mmparfum-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mmparfum-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true